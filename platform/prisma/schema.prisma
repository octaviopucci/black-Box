generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Business entities (Organization, User, Lead, Sale, etc.) are added
// in missions 02+. See docs/black-box-platform/SPECIFICATION.md
